# Database connection string
# Default: sqlite:terma.db
DATABASE_URL=sqlite:terma.db

# Server bind address and port
# Default: 0.0.0.0:3000
BIND_ADDR=0.0.0.0:3000

# Public host/domain for the install script and join commands
# This is the URL users will see in install commands
# Examples: localhost:3000, terma.example.com, your-server.fly.dev
# Default: localhost:3000
HOST=localhost:3000

# GitHub repository for binary downloads (format: owner/repo)
# When set, the server will redirect downloads to GitHub Releases
# If not set, it will serve from local target/release directory (dev mode)
# Example: GITHUB_REPO=username/terma
# Default: not set (local mode)
GITHUB_REPO=
