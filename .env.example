# ==============================================================================
# BUILD ENVIRONMENT (GitHub Actions - where binaries are compiled)
# ==============================================================================
# These variables must be set during `cargo build` to be compiled into binaries

# TERMA_DEFAULT_HOST - Compiled into client binary as the default connection host
# - REQUIRED in production builds (GitHub Actions)
# - Client uses this when run with: terma-client <room_id>
# - Examples: terma.example.com:443, your-server.fly.dev:443
# - Omit in local development (defaults to localhost:3000)
TERMA_DEFAULT_HOST=

# ==============================================================================
# RUNTIME ENVIRONMENT (Server - where terma-server runs)
# ==============================================================================
# These variables are read by the server at startup

# DATABASE_URL - PostgreSQL database connection string
# - REQUIRED
# - Format: postgresql://user:password@host/database?sslmode=require
# - For Neon: Get from Neon dashboard connection string
# - For local dev: postgresql://postgres:password@localhost/terma
# - Example (Neon): postgresql://user:pass@ep-example.us-east-2.aws.neon.tech/terma?sslmode=require
DATABASE_URL=postgresql://postgres:password@localhost/terma

# BIND_ADDR - Server bind address and port
# - REQUIRED
# - Format: <ip>:<port>
# - Use 0.0.0.0 to listen on all interfaces
# - Default: 0.0.0.0:3000
BIND_ADDR=0.0.0.0:3000

# HOST - Public hostname/domain for install commands and URLs
# - REQUIRED in production
# - Used in install script URLs shown to users
# - Format: <hostname>:<port> or <hostname> (if using standard port)
# - Examples: localhost:3000, terma.example.com, your-server.fly.dev
# - Default: localhost:3000
HOST=localhost:3000

# GITHUB_REPO - GitHub repository for binary downloads (optional)
# - OPTIONAL
# - Format: owner/repo
# - When set: server redirects downloads to GitHub Releases
# - When not set: server serves from local target/release directory (dev mode)
# - Example: username/terma
# - Default: not set (local mode)
GITHUB_REPO=
