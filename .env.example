# Server host/domain for the install script
# Examples: localhost:3000, terma.example.com
HOST=localhost:3000

# GitHub repository for binary downloads (format: owner/repo)
# When set, the server will redirect downloads to GitHub Releases
# If not set, it will serve from local target/release directory (dev mode)
# Example: GITHUB_REPO=username/terma
GITHUB_REPO=
